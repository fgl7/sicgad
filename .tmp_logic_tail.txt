logic.md lines 559

- Dashboard de desempe±o por planta: series mensuales, comparaciones, ranking y explicaci¾n de cßlculo (numerador/denominador).
- Alertas por umbrales (configurables por indicador) y detecci¾n de faltantes (indicadores ôno calculablesö por ausencia de variables mÝnimas).
- Exportables: CSV/Excel y, mßs adelante, PDF (ficha mensual para fiscalizaci¾n).


## Plantillas HTML principales

Se sugiere organizar las plantillas de la siguiente manera:

### Base y layout general

- `templates/base.html`
  - Estructura HTML principal (header, barra lateral, zona de contenido).
  - Inclusi¾n de CSS (Tailwind) y JS global.

- `templates/partials/sidebar.html`
  - Men· lateral con enlaces a las principales secciones.

- `templates/partials/topbar.html`
  - Barra superior con tÝtulo de la vista, selector de planta y filtros.

### M¾dulo de autenticaci¾n (`accounts`)

- `templates/accounts/login.html`
- `templates/accounts/change_password.html`
- `templates/accounts/user_list.html`
- `templates/accounts/user_form.html`

### M¾dulo de esquemas (`schemas`)

- `templates/schemas/schema_list.html`
- `templates/schemas/schema_detail.html`
- `templates/schemas/schema_edit.html`
- `templates/schemas/change_requests.html` (solicitudes de cambio pendientes).

### M¾dulo de carga (`ingest`)

- `templates/ingest/upload.html` (subida de archivos).
- `templates/ingest/upload_history.html` (historial de envÝos del usuario).
- `templates/ingest/error_report.html` (detalle de errores de validaci¾n).

### M¾dulo de validaci¾n (`validation`)

- `templates/validate/inbox.html` (bandeja de datasets a revisar).
- `templates/validate/detail.html` (detalle de un dataset para aprobar/rechazar).

### M¾dulo de KPIs (`kpis` o `dashboards`)

- `templates/kpis/charts.html`
  - Vista principal de grßficos.
  - Selector de variables, rango de fechas y planta.
- `templates/kpis/export.html` (opcional, para descargas especÝficas).

### M¾dulo de auditorÝa (`audit`)

- `templates/audit/events.html` (lista de eventos auditados).
- `templates/audit/event_detail.html` (detalle de un evento crÝtico).

---
